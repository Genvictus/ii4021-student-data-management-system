spring.application.name=app
server.port=3000

spring.datasource.url=jdbc:postgresql://localhost:5433/postgres
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.default_schema=public

# Keystore configuration to store JWT signing key
keystore.path=${KEYSTORE_PATH}
keystore.password=${KEYSTORE_PASSWORD}
keystore.alias=${KEY_ALIAS}

# Uncomment this to automatically generate SQL schema based on Entity classes on startup
#spring.jpa.properties.jakarta.persistence.schema-generation.scripts.action=create
#spring.jpa.properties.jakarta.persistence.schema-generation.scripts.create-target=src/main/resources/postgres/schema.sql
#spring.jpa.properties.jakarta.persistence.schema-generation.scripts.create-source=metadata

# Uncomment thsi to initialize (seed) the database on startup
#spring.jpa.defer-datasource-initialization=true
#spring.sql.init.mode=always
#spring.sql.init.data-locations=classpath:/postgres/data.sql